sudo: required
services:
  - docker

before_install:
  - docker build -t dsaenz86/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run dsaenz86/react-test npm test -- --coverage

after_success:
  - docker build -t dsaenz86/multi-client ./client
  - docker build -t dsaenz86/multi-nginx ./nginx
  - docker build -t dsaenz86/multi-server ./server
  - docker build -t dsaenz86/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dsaenz86/multi-client
  - docker push dsaenz86/multi-nginx
  - docker push dsaenz86/multi-server
  - docker push dsaenz86/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-east-1
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-east-1-663432637386
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
