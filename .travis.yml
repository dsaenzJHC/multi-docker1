sudo: required
services:
  - docker

before_install:
  - docker build -t dsaenzjhc/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run dsaenzjhc/react-test npm test -- --coverage

after_success:
  - docker build -t dsaenzjhc/multi-client ./client
  - docker build -t dsaenzjhc/multi-nginx ./nginx
  - docker build -t dsaenzjhc/multi-server ./server
  - docker build -t dsaenzjhc/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dsaenzjhc/multi-client
  - docker push dsaenzjhc/multi-nginx
  - docker push dsaenzjhc/multi-server
  - docker push dsaenzjhc/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-east-1
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-east-1-663432637386
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
